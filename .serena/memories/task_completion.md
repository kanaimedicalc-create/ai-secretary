# Task Completion Checklist

Run from `todo-next/` in order:

1. `npm run lint` — ESLint (must pass clean)
2. `npm test` — Vitest full suite (must pass)
3. `npm run build` — Next.js build + type check (must succeed)
